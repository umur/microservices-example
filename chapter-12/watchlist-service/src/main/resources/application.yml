server:
  port: 8083
  shutdown: graceful

spring:
  application:
    name: watchlist-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/watchlist}
    username: ${DB_USER:cinetrack}
    password: ${DB_PASSWORD:cinetrack}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: ${OTLP_ENDPOINT:http://localhost:4318/v1/traces}
